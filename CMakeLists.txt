PROJECT(OPENALPP)
IF(UNIX)
  SET(INCLUDEDIRS /usr/include /usr/local/include)
ELSE(UNIX)
  SET(INCLUDEDIRS C:\)
ENDIF(UNIX)

FIND_PATH(PORTAUDIO_INCLUDE portaudio.h ${INCLUDEDIRS})
FIND_PATH(VORBIS_INCLUDE vorbis/vorbisfile.h ${INCLUDEDIRS})
IF(UNIX)
SET(CMAKE_CXX_FLAGS ${CMAKE_CXX_FLAGS} "-I`ccgnu2-config --includes`")
ELSE(UNIX)
#SET()
ENDIF(UNIX)

INCLUDE_DIRECTORIES(${PORTAUDIO_INCLUDE} ${VORBIS_INCLUDE} ../include)

IF(UNIX)
  SET(LIBDIRS /usr/lib /usr/local/lib)
ELSE(UNIX)
  SET(LIBDIRS C:\)
ENDIF(UNIX)
LINK_DIRECTORIES(${LIBDIRS})

SUBDIRS(src tests)
